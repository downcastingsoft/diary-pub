---
title: 日記
---
<h1>日記</h1>
<h2>記事一覧</h2>
<ul>
  <% sorted_articles.each do |item| %>
    <% d = item[:created_at] %>
    <% path = "/article/%04d/%02d/%02d/%s/" % [d.year, d.month, d.mday, item[:slug]] %>
    <li>
      <a href=".<%= path %>">
        <%= d.year %>/<%= d.month %>/<%= d.mday %>: <%= item[:title] %>
      </a>
    </li>
  <% end %>
</ul>